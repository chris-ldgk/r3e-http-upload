{
  "name": "race-result-upload",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "compile:win": "bun build src/index.ts --compile --minify --sourcemap --target=bun-windows-x64-baseline --outfile bin/race-result-upload"
  },
  "dependencies": {
    "hono": "^4.6.15"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}